# Tutorial Routing Rules

- 規格釐清：Primary `requirements-clarity`，Supporting `architecture-designer`。
- API 契約：Primary `api-designer`，Supporting `qa-test-planner`。
- Vue 實作：Primary `vue-expert`，Supporting `playwright`。
- 除錯：Primary `systematic-debugging`。
- 文件：Primary `code-documenter`。

這些名稱只來自 checked-in example catalog，不表示 runtime 可用。
