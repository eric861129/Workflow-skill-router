# Legacy Tutorial Capability Catalog

Legacy/tutorial example — not runtime discovery and not real-model evidence.

這個目錄只用 repo 內的八個公開示範 capability，協助理解 Primary/Supporting route。它不是 V2 runtime snapshot、不可代表本機已安裝能力，也不能用來宣稱模型評測結果。

V2 的真實來源是 runtime capability discovery；此範例僅供教學與 V1 相容閱讀。
