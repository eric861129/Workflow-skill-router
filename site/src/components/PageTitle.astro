---
import DefaultPageTitle from '@astrojs/starlight/components/PageTitle.astro';

const pathname = Astro.url.pathname.replace(/\/+$/, '');
const isLandingPage = pathname === '/Workflow-skill-router' || pathname === '/Workflow-skill-router/zh-tw';
---

{!isLandingPage && <DefaultPageTitle />}
