---
import MobileTableOfContents from 'virtual:starlight/components/MobileTableOfContents';
import TableOfContents from 'virtual:starlight/components/TableOfContents';

const locale = Astro.locals.starlightRoute.locale || 'root';
const isZh = locale === 'zh-tw';
const basePath = isZh ? '/Workflow-skill-router/zh-tw' : '/Workflow-skill-router';
const repoUrl = 'https://github.com/eric861129/Workflow-skill-router';

const copy = isZh
  ? {
      eyebrow: 'V2 開源專案',
      title: '選擇你的安裝模式',
      body: '需要 runtime 能力時安裝 Plugin + MCP；只有 instructions 的環境使用純 SKILL。',
      star: '查看 GitHub',
      install: '查看安裝方式',
    }
  : {
      eyebrow: 'V2 open source',
      title: 'Choose an install mode',
      body: 'Use Plugin + MCP for runtime capabilities or Skill-only for instruction-only hosts.',
      star: 'View on GitHub',
      install: 'Compare installs',
    };
---

{
  Astro.locals.starlightRoute.toc && (
    <>
      <div class="lg:sl-hidden">
        <MobileTableOfContents />
      </div>
      <div class="right-sidebar-panel sl-hidden lg:sl-block">
        <div class="sl-container">
          <TableOfContents />
          <aside class="wsr-doc-cta" aria-label={copy.title}>
            <span>{copy.eyebrow}</span>
            <h2>{copy.title}</h2>
            <p>{copy.body}</p>
            <a class="wsr-doc-cta-primary" href={repoUrl} target="_blank" rel="noreferrer">
              {copy.star}
            </a>
            <a class="wsr-doc-cta-secondary" href={`${basePath}/guides/quickstart/`}>
              {copy.install}
            </a>
          </aside>
        </div>
      </div>
    </>
  )
}
